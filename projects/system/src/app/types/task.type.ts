export type TaskType = {
    title: string;
    responsable: string;
    points: number;
    startDate: Date;
}

export type SendData = {
    title: string;
    responsable: string;
    points: number;
    startDate: Date;
}